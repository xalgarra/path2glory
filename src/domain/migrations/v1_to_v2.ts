import type { UserDataEnvelope } from '../hero/types'

/**
 * Placeholder migration from schema v1 → v2.
 * Implement the actual transformation when USER_DATA_SCHEMA_VERSION is bumped to 2.
 */
export function v1ToV2(data: UserDataEnvelope): UserDataEnvelope {
  return data
}
